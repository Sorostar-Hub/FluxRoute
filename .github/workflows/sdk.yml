name: SDK CI/CD

on:
  push:
    branches: [main, develop]
    paths:
      - 'packages/sdk/**'
      - 'packages/*/package.json'
      - '.github/workflows/sdk.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'packages/sdk/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-and-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Lint SDK
        run: npm run lint --filter=sdk
      - name: Type-check SDK
        run: npx tsc --noEmit -p packages/sdk/tsconfig.json

  build:
    runs-on: ubuntu-latest
    needs: lint-and-typecheck
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Build SDK
        run: npm run build --filter=sdk
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sdk-build
          path: packages/sdk/dist
          retention-days: 1

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Run SDK tests
        run: npm run test --filter=sdk
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          files: ./packages/sdk/coverage/lcov.info
          flags: sdk
          fail_ci_if_error: false

  validate-exports:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Download build
        uses: actions/download-artifact@v4
        with:
          name: sdk-build
          path: packages/sdk/dist
      - name: Test imports
        run: node -e "const sdk = require('./packages/sdk/dist/index.cjs'); console.log('SDK imported successfully', Object.keys(sdk).length, 'exports')"
